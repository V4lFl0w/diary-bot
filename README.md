# ValFlow Diary Bot — MVP

## Быстрый старт
1) Скопируй .env.example → .env и заполни токен/БД.
2) Подними Postgres (локально или через Docker) и создай БД valflow_diary.
3) python -m venv .venv && source .venv/bin/activate (Windows: `.venv\\Scripts\\activate`)
4) pip install -r requirements.txt
5) python -m app.main

## Команды в боте
- /start — онбординг
- /journal — подсказка для записи
- /stats — краткая статистика
- /remind — пример синтаксиса
- /premium — статус премиума

## Дизайн решений
- Только 3 постоянные кнопки (🌐 Language / 🔒 Privacy / 🛠 Report bug).
- Премиум-день выдаётся автоматически после 2 записей + 1 напоминания + подписки на Telegram канал.
- Напоминания хранятся в БД, фоновая задача проверяет due раз в 30с.
- i18n простая, JSON-файлы.

## Куда развиваться дальше
- Экспорт PDF/CSV (включить реальный генератор PDF);
- Конструктор cron («каждый день в 9:00» → `cron`);
- Платежи Telegram Stars/Stripe;
- Веб-дашборд (Next.js) с графиками и поиском;
- Политика: кнопка «кризисные ресурсы» при триггерах.